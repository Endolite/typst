#set document(title: "Topology — Munkres")
#import "preamble.typ": *
#show: preamble
#align(center + horizon, box[#text(20pt)[Topology -- Munkres] \ \ Arnav Patri]) <no-header>
#pagebreak()
#align(center + horizon, text(16pt)[Chapter 2 -- Topological Spaces and Continuous Functions]) <no-header>
  #include "2/13.typ"
  #include "2/16.typ"
