#set document(title: "Topology — Munkres")
#import "preamble.typ": *
#show: preamble
#align(center + horizon, box[#text(20pt)[Topology -- Munkres] \ \ Selected Exercise Solutions by Arnav Patri]) <root>
  #pagebreak()
  #toc(false)
  #include "notation.typ"
  #include "gen/p1.typ"
