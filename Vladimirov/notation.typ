#import "preamble.typ": *
#show: preamble
= Notation
This document uses the following conventions:
-
  $0 in NN$.
-
  Let $(X, <=)$ be a poset.
  -
    Zero is denoted $bb(0)$ while unity is denoted $bb(1)$, when they exist, and may be subscripted with $X$.
  -
    If $x in X$ has a complement, it is denoted $not x$.

-
  Let $X$ be a Boolean algebra.
  -
    The symmetric difference between $x, y in X$ is denoted $x plus.circle y$.
